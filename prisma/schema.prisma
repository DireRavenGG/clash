generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://vcatwdosahwujh:8ad597f81d2ad69564f6253c7590dd3623096e9722f13f2efb7b21a3c73dface@ec2-52-204-29-205.compute-1.amazonaws.com:5432/df3sgka0afpaq7"
  }

model Player {
  id String @unique
  userName String 
  summonerLevel Int
  profileIconId Int
  matches Match[]
  matchlist MatchId[]
}
// Make sure to add something for Kill Participation 
// Was passing down Match to frontend
// seems bad. need way to save to DB.
model Match{
  user Player @relation(fields: [userId], references: [id])
  id Int @id @default(autoincrement())
  userId String 
  kills String
  deaths String
  assists String
  win Boolean
  champion String
  summonerSpell1 Int
  summonerSpell2 Int
  cs Int
  gameLength Int
  level Int
  kp Int
}

model MatchId {
  user Player @relation(fields: [userId], references: [id])
  userId String
  id Int @id @default(autoincrement())
  matchId String @unique

}

// model champStats {
//  id , championId, wins, matches, k, d ,a , cs, userId 
//}